// we import modules that have a default name without curly brace
// here we imported greet without curly brace because it was exported with default name
import greet from "./main.js";

console.log(greet());