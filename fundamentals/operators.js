// The prefix form returns the new value while the postfix form 
// returns the old value (prior to increment/decrement).

let age = 1;

console.log(`age: ${age++}`);
console.log(`age: ${age++}`);

let marks = 1;

console.log(`marks: ${marks++}`);
console.log(`marks: ${marks++}`);